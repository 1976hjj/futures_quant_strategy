# M4.6 Execution Evidence 独立验收

状态：`PASS_WITH_FINDINGS`。

独立审计验证了四个 Parquet 的内容哈希和行数、Evidence ID 与请求身份、M2-E 核心 vintage、score 数量、日度成本不变量、拒单总数对账、实体摘要复算、换手范围以及三档资金的成交率/成本单调性。

验收结果：14 条 score 路径，所有审计失败项为 0。100 万、1,000 万、1 亿元的跨 score 平均成交率依次为 98.7835%、98.7085%、98.0353%，平均成本依次为 15.1683、15.5238、16.5566 bps。

发现项：共有 4,216 个“资金场景×订单”缺少可用的退市终止收益，系统显式记录为 `DELISTING_RETURN_UNAVAILABLE`，没有假设收益为零。该样本是已暴露研究窗口，且日线涨跌停规则无法观察真实排队成交，所以验收通过不代表策略可晋级或结果可视为实盘收益。
