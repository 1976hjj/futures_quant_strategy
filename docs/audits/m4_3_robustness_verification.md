# M4.3 稳健统计证据独立验收

状态：PASS  
验收日期：2026-09-04

## 验收对象

- Robustness ID：`sha256:a8368caf70682a1918fb3f2c7380e510b2b62a169db1e142a603d0d601337eaa`；
- 39 个因子/变体假设；
- 117 个固定时间段统计；
- HAC、循环 moving-block bootstrap、BH-FDR 与不可变 Manifest。

## 独立复算

审计使用与生产入口分离的实现，重算 RAW `book-to-price` 的 Newey–West/Bartlett 长期方差、双侧渐近 p 值、10,000 次循环块自助法 p 值和区间。随后独立对全部 39 个 p 值执行 BH 反向单调校正。数值与发布资产在 `1e-12` 容差内一致。

文件哈希、输入 Evidence Manifest 哈希、数据库登记、唯一键、概率范围、家族行数和稳定性分段数均通过。全量家族中 bootstrap 与 HAC 的 FDR 拒绝数都为 0。

## 审计结论

PASS 只证明统计实现和不可变证据链符合冻结规格。58 个交易日属于短样本，HAC 使用渐近参考，三个变体高度相关，且当前没有 OOS、涨跌停、退市和成本模型。任何因子都不得因本阶段结果晋级；“没有拒绝零假设”也不得解释成已经证明因子无效。
