# M4.5 去重与增量价值独立验收

验收状态：`PASS_WITH_FINDINGS`  
审计报告：`reports/m4_5_redundancy_audit.json`

## 验收对象

- Redundancy release：`sha256:442cf98e81eb98eea2b41714ed1d9d5a6fb449b905e6805766f7f8f3c93a0626`；
- 39 条因子×变体路径、741 个路径对；
- 53,352 条月末截面相关；
- 25 条 canonical 路径、10 个信息簇；
- 36,375 条日度条件统计、25 个统一检验假设。

## 独立检查

审计未调用生产聚类与偏相关函数。它独立核对资产及上游哈希、数据库注册、暴露账本快照、路径对唯一性、相关边界、双阈值去重、完整层次树、每簇唯一代表和日度条件统计，并从原始因子发布与标签重新计算固定日的秩、相关矩阵、半偏/偏 RankIC 和增量 R²。

固定截面的因子值相关、全窗口 IC 相关和条件统计均与发布资产一致。M4.5 前后 Holdout 暴露账本保持 3 条 M4.4 记录，没有新增 M4.5 暴露事件。

## 必须保留的发现

- 13 组 RAW/`WINSORIZED_ZSCORE` 是近重复路径，不能分别计数；
- `book-to-price` 的规模中性变体也达到冻结去重阈值；
- 10 个机械簇代表不等于 10 个有效 Alpha，也没有任何代表被晋级；
- volume-shock 与反向 momentum 的方向均受 M4.4 后选择影响；
- return-volatility 的条件方向证据仍未经过成本和执行约束；
- 2020～2025 全区间已经暴露，所有 M4.5 结果只能称为诊断证据。

因此审计通过只代表资产、去重、聚类与条件统计可复现。最终决定保持 `NO_PROMOTION_REDUNDANCY_DIAGNOSTIC`。
