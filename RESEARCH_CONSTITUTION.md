# Alpha Research OS 研究宪法

版本：1.0.0-draft  
生效范围：所有正式数据、因子、模型、策略、回测、报告和 Research Agent  
机器可读规则：`constitution/research_rules.yaml`

## 1. 权威与修改权限

本宪法的优先级高于实现代码、实验配置、Agent 指令和收益目标。

- Research Agent、代码生成器、Evaluator 和自动修复程序只有读取权限；
- 自动化任务不得修改、弱化、跳过或重新解释这些规则；
- 修改必须由项目所有者明确批准，产生新版本和变更理由；
- 旧实验永久绑定其运行时宪法版本；
- 为使实验通过而事后修改规则、阈值或检验家族，属于研究违规。

## 2. 时间与信息规则

### RULE-001 禁止未来数据

因子、Universe、预处理、参数和交易决定不得使用决策时点以后才可知的信息。

### RULE-002 以信息可用时间为准

任何 T 日信号只能使用 `available_at <= signal_cutoff` 的数据。经济事实所属日期不等于其可用日期。

### RULE-003 收盘信息不得按同一收盘价成交

若信号使用 T 日收盘后才能确认的数据，默认最早进入 T+1 的首个合法可成交事件。任何例外必须有独立的盘中信息和成交模型证据。

### RULE-004 财务数据按实际披露进入系统

财务数据必须保留报告期、首次披露、可用时间和修订版本。禁止在真实披露以前进入历史研究。

### RULE-005 Feature 与 Label 隔离

Feature runtime 禁止未来引用，且无权读取 Label 数据。未来引用只允许存在于独立 Label runtime。

## 3. 历史 Universe 与数据规则

### RULE-006 股票池必须 Point-In-Time

股票池构建不得使用未来成分股或未来加入/剔除信息。

### RULE-007 退市证券不得从历史消失

历史研究必须保留当时存在、后来退市的证券及可实现的退市收益处理。

### RULE-008 状态必须历史化

禁止使用今天的 ST、停牌、行业、板块或证券名称状态过滤历史样本。

### RULE-009 公司行为与复权必须可追溯

复权价格不得隐含研究时点未知的未来公司行为。原始价格、公司行为和复权规则必须分别保存和版本化。

### RULE-010 数据错误阻断依赖研究

Data Audit 的 blocker 必须停止依赖受影响数据域的正式研究。系统不得把数据错误解释为因子失败。

## 4. 样本划分与选择规则

### RULE-011 Test 不参与选择

最终 Test 和 Walk-Forward Test 不得用于因子、方向、参数、预处理、模型或阈值选择。

### RULE-012 预处理仅在允许样本拟合

任何需要估计参数的缺失处理、标准化、编码、降维和模型步骤，只能在 Train 拟合并冻结后应用于 Validation/Test。

### RULE-013 重叠标签必须 purge/embargo

当 forward label 跨越数据切分边界时，必须按 LabelSpec 执行 purge，并按预注册配置执行 embargo。

### RULE-014 因子方向不能全样本决定

若因子正负方向不是经济假设预先规定，只能在 Train 内确定并冻结。

### RULE-015 所有搜索必须记录

参数、窗口、Universe、horizon、预处理、因子、模型和组合方法的尝试均属于搜索，必须记录，包括失败和人工查看过的结果。

### RULE-016 Holdout 暴露不可逆

任何人或进程读取 Holdout 后必须写入暴露账本。已暴露区间不得再次声称为未见样本。

## 5. 统计规则

### RULE-017 单个 p-value 不能证明有效

大量候选同时测试时，禁止仅以原始 `p < 0.05` 判定有效。

### RULE-018 检验家族必须预注册

Multiple-testing family 必须在看结果前定义，并覆盖相关因子、参数、horizon、variant、Universe 和连续研究轮次。

### RULE-019 相关样本不得假装独立

重叠收益、滚动窗口、重叠 Fold、横截面共同冲击和序列相关必须进入标准误或重采样设计。

### RULE-020 事后 Regime 只用于诊断

使用完整未来路径定义的 Bull/Bear/Sideways 只能用于鲁棒性说明。若用于交易，必须建立独立 PIT Regime 模型并重新 OOS 验证。

### RULE-021 异常优秀结果强制审计

异常高 IC、收益或 Sharpe 必须进入 Suspicious Result Audit，首先检查泄漏、Universe、复权、成交、成本、搜索和暴露。

## 6. 交易可实现性规则

### RULE-022 Signal、Order、Fill 必须分离

策略收益必须可追踪到信号冻结、目标组合、订单、可成交判断、成交和持仓。不得直接把未来收益乘信号当作最终策略回测。

### RULE-023 A 股交易规则必须按历史生效

T+1、涨跌停、ST、停牌、上市初期、退市整理期、交易单位和价格精度必须按当时规则处理。

### RULE-024 无成本结果不得标记 Tradable

没有手续费、印花税、滑点和换手测试，不得标记为 `TRADABLE`。

### RULE-025 无流动性和容量结果不得标记 Deployable

没有成交金额、参与率、冲击和多资金规模测试，不得标记为 `DEPLOYABLE`。

## 7. 记录与治理规则

### RULE-026 失败实验永久记录

所有失败、无效、冗余和被推翻的实验必须保留。禁止删除或只报告成功结果。

### RULE-027 原始 artifact 不得覆盖

Raw 数据、RAW 因子、ExperimentSpec、运行结果和审计记录只能追加新版本，不能原地重写历史。

### RULE-028 状态变化必须有证据事件

因子的 REJECTED、WATCH、CORE、DECAYED、RETIRED 等状态必须绑定具体实验和审批事件。

### RULE-029 无 OOS 不得标记 Validated

没有严格 OOS 结果的因子、模型或策略不得标记为 `VALIDATED`。

### RULE-030 审计门槛不得因结果改变

不得因为实验未通过而降低阈值、缩小检验家族、改变样本或重新定义指标后继续声称原实验通过。

### RULE-031 报告不是事实来源

报告必须由结构化 Evidence Bundle 生成，不得人工修改报告数字后反写 Registry。

### RULE-032 外部实现必须独立验证

开源项目、论文、供应商和公开因子的实现不能因来源权威而免于单元测试、人工样例、交叉验证和 OOS 审计。

## 8. Agent 权限规则

### RULE-033 Agent 是受限研究用户

Agent 可以提出假设、实现因子、设计实验、分析结果和生成报告，但不得拥有管理员权限。

### RULE-034 Agent 禁止修改事实与规则

Agent 禁止修改宪法、历史实验、原始/PIT 数据、失败档案、审计 findings 和锁箱暴露记录。

### RULE-035 Agent 禁止读取未释放 Holdout

禁止通过直接路径、缓存、汇总统计、错误日志或旁路工具读取或推断未释放 Holdout。

### RULE-036 Agent 不得自行晋级

Agent 可以提交晋级建议，但 CORE/DEPLOYABLE 状态必须由确定性 gate 和授权审批产生。

## 9. 解释原则

- 无法证明数据在当时可用时，按不可用处理；
- 无法证明成交可实现时，按未成交或保守成交处理；
- 无法区分研究改进与 Test 反馈时，按 Test 已被污染处理；
- 历史回测永远不等于未来收益；
- “没有发现错误”不等于“证明没有错误”。
